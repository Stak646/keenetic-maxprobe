# Documentation (EN)

Modes:
- FULL (default) includes sensitive data, but produces SENSITIVE_LOCATIONS.md to help redact.
- SAFE redacts in copies, still produces the list.

Profiles:
- diagnostic (default)
- forensic (deep)

`fs/` mirrors absolute paths.
